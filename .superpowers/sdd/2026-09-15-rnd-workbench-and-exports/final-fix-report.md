# Final whole-plan fix wave — 2026-09-22

Base: `508dc59`. One implementer, no subagents, no branch switch/reset/push/production deployment. Scope: the seven Important findings and one Minor in `final-fix-brief.md`. The controller owns final scoped review and CUA; unrelated DemandDetailView, sibling report, cache and output changes are not staged. The controller explicitly authorized including its existing 14-line implementation-plan status update in this commit. Commit: the scoped changeset containing this report (SHA returned to controller).

## Per-finding RED → GREEN

1. **SOP completeness.** `ProcessExportCheckServiceTest.sopRequiresControlRequirementAndUsedParameterMeaningAndNumericUnit` initially failed: a control with no target/bounds/basis plus numeric `97` without name/unit produced no expected field-path issues. `mvn -q -Dtest=ProcessExportCheckServiceTest,SopDocumentRendererTest test` RED: 11 tests, 2 failures, 0 errors (other failure was finding 8), `/private/tmp/final-wave-gate-red.log`. The gate now checks used parameter meaning/applicable numeric units and each control's identity, numeric-unit/qualitative requirement, method/frequency/deviation action. Paths identify parameter fields and control sequence/field. Explicit `无量纲` is accepted; qualitative requirements need no numeric target and render their recorded basis instead of an erroneous missing-target placeholder. The existing shared check endpoints and formal `requireReady` call remain; incomplete previews and existing archive downloads remain available. No food standard is invented.
2. **Orphan planned keys on graph deletion.** Mounted save regression RED: removed material still sent `{mat:10}` (not `{}`); then GREEN for material, step and major deletions. `replacePlan` prunes only the three node-keyed planned maps against remaining stable IDs, preserving surviving targets and batch-level fields. Backend reference validation is unchanged.
3. **Inherited actual provenance.** `TrialPromotionServiceTest.inheritedExportProvenanceComesFromPromotionSnapshotNotMutableTrial` RED: `试验方案 继承方案B V3` lacked the inheritance label (`/private/tmp/final-wave-provenance-red.log`, 1 failure). Shared export metadata now carries only inheritedActuals/sourceTrialId in addition to existing metadata. Revision/locked-pricing provenance is extracted from immutable `experiment_trial_promotion.trial_snapshot_json`, not current trial JSON. The regression mutates current trial inheritance after submission and confirms formal label remains unchanged; unauthorized tester/finance draft/formal reads remain denied, and existing pinned-tester coverage passes. SOP/formula/pricing visibly say inherited observations are for comparison and not new observations; body labels say 方案记录 for inherited data. Internal source IDs are not rendered.
4. **Compact A/B overview.** Pure regression RED: changed purpose/variables/conclusion/recommendation yielded no corresponding differences. GREEN: they remain visible; parallel cards display purpose, variables, status/conclusion, reason, external actual total and final actual yield. Detailed values and parameter/recipe differences remain collapsed. Missing/invalid-basis actuals remain pending/incomparable; no automatic ranking.
5. **Numeric parameter delta.** Pure RED: planned `95`, actual `97` returned null; mounted RED displayed `实际：97℃ · 不同`. Initial GREEN used an explicitly passed unit; controller CUA then exposed the actual slot invocation without a unit (`实际：97 · 偏差：+2`). The mounted test now uses that real no-unit argument path, confirmed RED in `/private/tmp/final-wave-unit-red.log`; the display helper reads its row's owning-step unit and focused GREEN is 27/27. `+2℃` uses the same step parameter definition/unit, strict complete finite numeric strings, and shared delta rounding. Text shows change, no plan says 未设计划, no actual says 待补充. Partially numeric text is not coerced, and existing A/B name/unit mismatch tests still pass. Planned parameter records intentionally share their owning step's name/unit definition; no second conflicting definition is synthesized.
6. **Missing actual summary as zero.** Mounted RED: missing primary actual was `0`, expected null. GREEN covers absent/structure-copy observations, partially missing external total, observed zero and `0.0001kg`. A UI-only actual-summary adapter checks completeness independently for primary input, terminal primary output, balance and external total; incomplete sums show 待补充. Genuine zero stays zero, small positive weight stays visible. Formal positive-input policy and shared submission validators are untouched.
7. **Long-name page overflow.** Controller RED evidence: three long names expanded document to 1789px at 1091px viewport. Workbench grid children now have bounded min-width, tabs scroll inside their own flex area, action groups wrap, and long headings wrap without removing full names/accessibility text. First CUA GREEN confirmed documentWidth=viewportWidth=1091 and visible main actions, but entering steps exposed an existing implicit grid min-content issue clipping control-card tools/actions. The control editor now bounds its grid tracks and input widths, wraps header/confirmation actions, and uses two columns at ≤1100px instead of hiding excess width. Final CUA result is recorded below.
8. **Chinese retest labels.** The existing renderer fixture now uses actual enum `PASS`; RED expected `复测结果：符合` but output contained PASS. Renderer routes PASS/FAIL/PENDING through its established Chinese result map, retaining free-text retest notes. GREEN fixture verifies 符合 and absence of raw PASS.

Frontend initial RED commands: `node --experimental-strip-types --test tests/trial-comparison.test.mts` (21 tests, 2 failures) and `node --experimental-strip-types --test tests/trial-workbench-mounted.test.mts` (6 tests, 2 failures). The deletion regression was run again after moving the independent delta assertion, specifically observing the orphan-key failure. Focused combined GREEN: 27/27. Final backend focused command covering ProcessExportCheckServiceTest, SopDocumentRendererTest, TrialPromotionServiceTest, ProcessArtifactServiceTest: 54 tests, 0 failures/errors; `/private/tmp/final-wave-focused.log`.

## Fresh whole-wave verification

**Final gate after stable CUA, 2026-09-24:** `mvn -q test` completed with **364 tests, 0 failures/errors/skipped**; frontend whole suite completed with **159/159**, 0 failed/cancelled/skipped/todo. Contracts, routes (PC 30/mobile 16), shared/PC/mobile typecheck and PC/mobile production build all exited 0. These final runs include the last step-editor grid and inherited footer changes. Logs: `/private/tmp/final-wave-backend-final-0924.log`, `final-wave-frontend-final-0924.log`, `final-wave-contracts-final-0924.log`, `final-wave-routes-final-0924.log`, `final-wave-typecheck-final-0924.log`, `final-wave-build-final-0924.log`. Earlier complete runs below remain historical evidence rather than the latest code gate; the single 152 API run is retained because subsequent changes are display/layout only.

- `backend: mvn -q test` → exit 0; Surefire aggregate **364 tests, 0 failures/errors**, `/private/tmp/final-wave-backend-full.log`.
- `frontend: node --experimental-strip-types --test tests/*.test.mts` → exit 0; **159/159**, 0 failed/cancelled/skipped/todo, `/private/tmp/final-wave-frontend-full.log`.
- `pnpm check:api-contracts`, `pnpm check:routes`, `pnpm typecheck`, `pnpm build` → exit 0; PC 30/mobile 16 routes; shared/PC/mobile types; PC/mobile builds. Build log `/private/tmp/final-wave-frontend-build.log`.
- Project preview script `stop → build → start → status` → latest build running, 8082/5183/5184 all 200; Feishu remains disabled. Sandbox process/network checks initially could not identify runtime; authorized stop/build/start/status then completed normally. No database/file reset. `/private/tmp/final-wave-preview-build.log`.
- One fresh `scripts/verify-rnd-trials.mjs` run with local configured credential only in ephemeral environment → **152/152**, exit 0, `/private/tmp/final-wave-api.log`. Labelled test-only TASK-0018 / EXP-0018, evidence `data/preview/Task6-2026-09-22T11-49-19-952Z`; no production records or notifications.
- `git diff --check` → exit 0.

Existing non-failing Flyway/H2, JVM/Mockito, Vue Router harness and Vite chunk warnings remain. No dependency upgrade or suppressed validator/error was used.

## Rendered artifacts / controller QA

Application-generated new fixtures under `backend/target/final-fix-export-qa/`:

- `inherited-qualitative-numeric-sop.docx`: inherited provenance, two majors, qualitative and numeric controls, target/range, missing/zero measurement and Chinese retest; **4 printed pages** inspected.
- `inherited-formula_xlsx.xlsx`: external actual 102kg / normalized 100kg, inheritance on both sheets; **2 printed pages** inspected, both sheets imported/rendered without error tokens.
- `inherited-pricing_xlsx.xlsx`: inherited source, 102kg basis/72% yield/independent 74kg and 74 bags; **3 printed pages** inspected; three sheets imported without error tokens.

Controller reported all **9 pages** readable with no new clipping/overlap, correct inheritance, qualitative requirements, numeric targets/ranges and retest labels. QA directories: `/private/tmp/rnd-export-qa-0922.8zDblM/final-sop`, `final-formula`, `final-pricing`. A known pricing empty-shared-string importer display quirk remains importer-only; actual printed blanks are correct. No further style redesign.

In-wave self-review then found the inherited formula's sheet tab and right footer still said 本次实际投料. Separate RED assertions captured both label paths (`/private/tmp/final-wave-tab-red.log`, `final-wave-footer-red.log`). They now consistently say 方案实际投料 for inherited observations only; ordinary files keep their prior labels. On 2026-09-24 resume the focused artifact test passed and rewrote the same inherited formula fixture for the controller's two-page recheck. This is not a layout redesign or a second fix wave.

Controller's updated formula QA passed: two sheets imported with zero error tokens, both printed pages inspected, tab/right footer both 方案实际投料, inherited provenance and 102kg actual versus 100kg normalization legible. Latest formula QA is `/private/tmp/rnd-export-qa-0922.8zDblM/final-formula-v2`.

The remaining control-card clip was traced by actual DOM geometry, not guessed from source: step-editor clientWidth 519 / scrollWidth 602 / implicit grid track 570px; the control region extended to x1082 beyond its visible parent's x1016, with the add-measurement button ending at x1061. The final local CSS adjustment bounds the **step-editor** track and direct children, wraps action groups, and moves the plan-material row's 536px minimum grid to a single column at 1100px. Focused frontend 27/27, typecheck and both builds passed after this change (`/private/tmp/final-wave-frontend-focused-0924.log`, `final-wave-typecheck-0924.log`, `final-wave-frontend-build-0924.log`); latest preview is rebuilt and healthy.

**Final CUA GREEN, 2026-09-24:** viewportWidth=documentWidth=1091; step-editor clientWidth=scrollWidth=519, grid track 487px; control card width 487/right x999, add-measurement right x978, both inside visible parent right x1015. Controller screenshot confirmed complete output/control/add-measurement actions with no clipping. Expanded plan shows 实际97℃ / 偏差+2℃; missing primary/external actuals show 待补充. Compact A/B overview had already passed with A 102kg/72%, B missing values pending, and detailed differences collapsed. Deletion/save is covered by mounted regressions, not claimed as a live persistent-record deletion test.

## Changed contracts and boundaries

- Additive formal source fields: inheritedActuals and sourceTrialId; only these fields are extracted from the stored promotion snapshot. No full trial/draft payload is exposed.
- Additive export metadata provenance and stricter SOP-only readiness issue codes/precise paths. Formula/pricing readiness and existing archive read authorization/integrity checks are preserved.
- Frontend planned map pruning and display-only summary/comparison changes do not change persisted trial DTOs or calculation policy.
- Browser download event delivery previously timed out after 15 seconds despite fulfilled handler and no console errors. No confirmed handler defect was found, so the handler is unchanged and browser file receipt is not claimed.
- Controller CUA and artifact QA are complete for this wave. The eight findings have scoped regression evidence, with layout additionally verified at the actual viewport. The controller's single final scoped rereview follows this commit; this report does not claim that independent review has already run.
