---
title: 研发样品管理系统执行记录
type: execution-log
tags:
  - 研发样品管理
  - 执行记录
  - 后端骨架
updated: 2026-06-18
---

# 研发样品管理系统执行记录

## 2026-06-18 第一轮执行

已创建开发分支：codex/rnd-sample-foundation。

已完成基础任务：

- RND-009 后端 Spring Boot 项目骨架：新增 RndSampleSystemApplication，Spring 上下文可启动。
- RND-016 统一接口返回格式：新增 ApiResponse。
- RND-017 全局异常处理：新增 BusinessException、GlobalExceptionHandler。
- RND-159 样品项目状态机雏形：新增 SampleStatus、SampleAction、SampleStatusMachine。
- RND-163 样品版本递增规则雏形：新增 SampleVersionCode，支持 A0、A1、A2、A3...A10...An。
- RND-098 核价 Excel 生成雏形：新增 PricingFileService，可生成基础核价工作簿并计算领料重量、参考包数。

当前业务边界：本项目不做工艺/BOM，流程终点为生成核价文件并通知财务核价。

验证结果：

- mvn test：9 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

下一步建议：从数据模型实体和持久化结构开始，优先实现 SampleRequest、SampleProject、SampleVersion、RndTask。

## 2026-06-18 第二轮执行

已完成主业务链路的内存版后端 API：

- 新增 SampleRequest、SampleProject、SampleVersion、RndTask、RndTaskStatus 等主链模型。
- 新增 SampleWorkflowService，支持研发内勤创建需求、研发总监审核通过、自动生成样品项目、自动生成 A0 样品版本、自动生成任务池任务。
- 新增 SampleWorkflowController，提供首批 REST API。

已实现接口：

- POST /api/v1/sample-requests：创建样品需求单。
- GET /api/v1/sample-requests：查看样品需求列表。
- POST /api/v1/sample-requests/{id}/approve：研发总监审核通过需求，生成项目、A0 版本和任务池任务。
- GET /api/v1/rnd-tasks/pool：查看研发任务池。
- POST /api/v1/rnd-tasks/{id}/assign：研发总监分发任务给具体研发人员。

已补充测试：

- SampleWorkflowControllerTest：覆盖创建需求、审核生成项目和 A0 版本、任务池查询、任务分发。
- 覆盖异常场景：审核不存在需求、分发不存在任务时返回业务错误码。

验证结果：

- mvn test：12 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前实现说明：

- 当前主链服务使用内存存储，目的是先跑通业务接口和状态语义。
- 下一步建议接 PostgreSQL/JPA 或 Flyway，把 SampleRequest、SampleProject、SampleVersion、RndTask 落成数据库表。
- 也可以先继续向后实现研发人员接受任务、实验单草稿、提交内部测试这段流程。

## 2026-06-18 第三轮执行

已完成“研发人员接受任务 -> 实验单草稿 -> 提交内部测试”的内存版后端 API。

新增模型：

- ExperimentForm：实验单主记录，挂到具体任务和样品版本。
- ExperimentFormStatus：DRAFT、SUBMITTED_FOR_TEST、LOCKED。
- TestAssignment：内部测试任务。
- TestAssignmentStatus：PENDING_TEST、PASSED、FAILED_RESAMPLE。

新增接口：

- POST /api/v1/rnd-tasks/{id}/accept：研发人员接受任务，任务进入 SAMPLING。
- POST /api/v1/rnd-tasks/{id}/experiment-form/draft：保存实验单草稿，支持物料明细。
- POST /api/v1/experiment-forms/{id}/submit-test：提交实验单给内部测试人员，生成内部测试任务。

业务规则：

- 只有 PENDING_ACCEPTANCE 状态的任务可以接受。
- 只有被分发的研发人员可以接受任务。
- 只有 SAMPLING 状态的任务可以填写实验单草稿。
- 只有 DRAFT 状态的实验单可以提交内部测试。
- 提交内部测试后，研发任务状态进入 PENDING_TEST。

验证结果：

- mvn test：13 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

下一步建议：

- 继续实现内部测试确认，通过后锁定实验单版本。
- 锁定后实验单不可覆盖修改，复打样时生成下一样品版本，如 A0 到 A1。

## 2026-06-18 第四轮执行

已完成“内部测试确认 -> 通过锁版 / 不通过复打样”的内存版后端 API。

新增模型：

- TestRecord：内部测试结果记录。
- PassInternalTestResult：内部测试通过返回对象。
- FailInternalTestResult：内部测试不通过复打样返回对象。

新增接口：

- POST /api/v1/test-assignments/{id}/pass：内部测试通过，锁定实验单，测试任务标记 PASSED，研发任务标记 COMPLETED。
- POST /api/v1/test-assignments/{id}/fail-resample：内部测试不通过，测试任务标记 FAILED_RESAMPLE，自动生成下一样品版本和复打样任务。

业务规则：

- 只有 PENDING_TEST 状态的内部测试任务可以确认。
- 只有被配置的测试人员可以确认测试结果。
- 测试通过后实验单状态变为 LOCKED，表示历史版本只读不可覆盖。
- 测试不通过复打样时，版本号按 A + 数字递增，例如 A0 自动生成 A1。
- 复打样任务会沿用原研发负责人，状态进入 PENDING_ACCEPTANCE，等待研发再次接受任务。

验证结果：

- mvn test：15 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前后端主流程已跑通到：

研发内勤录需求 -> 总监审核 -> 生成 A0 版本 -> 任务池 -> 分发任务 -> 研发接受任务 -> 保存实验单草稿 -> 提交内部测试 -> 测试通过锁版 / 测试不通过生成 A1 复打样。

下一步建议：

- 实现寄样反馈列表和核价文件生成入口。
- 或先将当前内存版主链迁移到 PostgreSQL/Flyway/JPA，避免继续扩展后数据无法持久化。

## 2026-06-18 第五轮执行

已完成“寄样反馈 -> 生成核价文件 -> 通知财务核价”的内存版后端 API。

新增模型：

- ShipmentRecord：寄样记录，挂到具体样品版本。
- ShipmentStatus：SHIPPED、FEEDBACK_PASSED、FEEDBACK_FAILED_RESAMPLE、STOPPED。
- CustomerFeedback：客户/业务反馈记录。
- CustomerFeedbackResult：PASSED、FAILED_RESAMPLE、STOPPED。
- PricingFileRecord：核价文件元数据，记录文件名、核价版本、文件大小和状态。
- PricingFileStatus：GENERATED、FINANCE_NOTIFIED。
- FinanceNotification：财务核价通知记录。
- FinanceNotificationStatus：SENT。

新增接口：

- POST /api/v1/sample-versions/{id}/shipments：对指定样品版本创建寄样记录。
- POST /api/v1/shipments/{id}/feedback：提交寄样反馈，通过、复打样或停止。
- POST /api/v1/sample-versions/{id}/pricing-files：对指定样品版本生成核价 Excel 文件记录。
- POST /api/v1/pricing-files/{id}/notify-finance：通知财务核价，记录通知状态。

业务规则：

- 寄样记录必须关联具体样品版本。
- 寄样前样品版本必须已有 LOCKED 状态的实验单，避免未通过内部测试的样品流出。
- 寄样反馈只能对 SHIPPED 状态的寄样记录提交一次。
- 生成核价文件前，样品版本必须已有 LOCKED 状态的实验单。
- 核价文件使用锁定实验单里的物料明细生成，避免草稿数据被误用。
- 同一样品版本多次生成核价文件时，核价版本自动递增：A0-核价V1、A0-核价V2。
- 通知财务后，核价文件状态变更为 FINANCE_NOTIFIED。

本轮还修复了测试环境问题：

- 新增 src/test/resources/mockito-extensions/org.mockito.plugins.MockMaker。
- 指定 Mockito 使用 mock-maker-subclass，避免本机 JDK 环境下 inline mock maker self-attach 失败影响测试运行。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：18 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前后端主流程已跑通到：

研发内勤录需求 -> 总监审核 -> 生成 A0 版本 -> 任务池 -> 分发任务 -> 研发接受任务 -> 保存实验单草稿 -> 提交内部测试 -> 测试通过锁版 / 测试不通过生成 A1 复打样 -> 寄样 -> 客户反馈 -> 生成核价文件 -> 通知财务核价。

下一步建议：

- 将内存版主链迁移到 PostgreSQL/Flyway/JPA，先落 SampleRequest、SampleProject、SampleVersion、RndTask、ExperimentForm、ShipmentRecord、PricingFileRecord。
- 同步补充文件归档模型 ArchiveFile，为实验单历史版本、测试记录、寄样记录、核价 Excel 文件建立统一归档目录。

## 2026-06-18 第六轮执行

已开始数据库持久化地基建设，完成 PostgreSQL/Flyway/JPA 的首版接入。

新增依赖与配置：

- spring-boot-starter-data-jpa：后端持久化框架。
- flyway-core：数据库版本迁移。
- flyway-database-postgresql：PostgreSQL Flyway 支持。
- postgresql：生产/部署环境 PostgreSQL 驱动。
- h2：测试环境内存数据库。
- application.yml：默认 PostgreSQL 配置，Hibernate ddl-auto=validate，Flyway 自动迁移。
- application-test.yml：测试环境 H2 + Flyway 配置。

新增数据库迁移：

- V1__create_rnd_sample_workflow_tables.sql。

首版已建核心表：

- sample_request：样品需求单。
- sample_project：样品项目/主档。
- sample_version：样品版本，支持 A0、A1、A2、A3 等版本。
- rnd_task：研发任务。
- experiment_form：实验单。
- experiment_material：实验物料明细。
- test_assignment：内部测试任务。
- test_record：内部测试记录。
- shipment_record：寄样记录。
- customer_feedback：客户/业务反馈。
- pricing_file：核价文件版本。
- finance_notification：财务核价通知。
- archive_file：归档文件。
- audit_log：审计日志。

关键数据库约束：

- sample_version 上增加 uk_sample_version_project_version，确保同一项目下版本号唯一。
- pricing_file 上增加 uk_pricing_file_version_pricing_version，确保同一样品版本下核价文件版本唯一。
- rnd_task、experiment_form、shipment_record、pricing_file、finance_notification 等核心链路均已加外键约束。
- 增加按名称、时间、状态、负责人、业务对象检索的基础索引。

新增 JPA 映射：

- SampleRequestEntity：样品需求单实体。
- SampleRequestRepository：样品需求单仓库，支持按 sampleNo 查询。

新增测试：

- SchemaMigrationTest：验证核心表、唯一约束、外键约束能通过 Flyway 建出。
- SampleRequestRepositoryTest：验证样品需求单可以通过 JPA 保存并按样品编号查询。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：20 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前说明：

- 本轮完成的是“数据库地基”和首个主业务对象的 JPA 可写验证。
- 现有业务 API 仍使用内存版 SampleWorkflowService，暂未切换到数据库读写。
- 下一步建议继续按主链顺序增加 JPA 实体和 Repository：SampleProject、SampleVersion、RndTask、ExperimentForm、ShipmentRecord、PricingFileRecord，然后再把 SampleWorkflowService 从内存 Map 迁移到数据库事务服务。

## 2026-06-18 第七轮执行

已将主链入口从“只有数据库表结构”推进到“接口调用后主对象真实落库”。

新增 JPA 映射：

- SampleProjectEntity：样品项目/主档实体。
- SampleVersionEntity：样品版本实体。
- RndTaskEntity：研发任务实体。
- SampleProjectRepository：样品项目仓库。
- SampleVersionRepository：样品版本仓库。
- RndTaskRepository：研发任务仓库。

服务层调整：

- SampleWorkflowService 增加 Spring 注入构造器，接入 SampleRequestRepository、SampleProjectRepository、SampleVersionRepository、RndTaskRepository。
- createRequest 创建样品需求后，同步写入 sample_request。
- approveRequest 审核通过后，同步写入 sample_project、sample_version、rnd_task。
- createRequest 和 approveRequest 增加事务边界，保证主链入口写库动作具备原子性。
- 保留原内存 Map 流程，现阶段属于渐进式迁移，避免一次性改动影响后续打样、测试、寄样、核价流程。

新增测试：

- SampleWorkflowControllerTest 新增 approvingRequestPersistsProjectVersionAndTaskToDatabase。
- 测试覆盖：研发内勤创建需求、研发总监审核通过后，数据库中能查到 sample_request、sample_project、sample_version、rnd_task。
- 同时验证 A0 版本号和 PENDING_ASSIGNMENT 任务状态已落库。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：21 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前主链持久化状态：

- 已落库：样品需求单、样品项目、样品版本、研发任务初始记录。
- 仍主要内存：任务分发/接受、实验单、测试记录、寄样反馈、核价文件、财务通知。

下一步建议：

- 继续迁移 RndTask 状态变更：assignTask 和 acceptTask 写回 rnd_task。
- 再迁移 ExperimentForm、ExperimentMaterial、TestAssignment，使打样实验单和内部测试任务具备数据库历史版本基础。

## 2026-06-18 第八轮执行

已完成“任务池 -> 分发 -> 研发接受”这段任务状态的数据库写回。

实体调整：

- RndTaskEntity 增加 assign 方法，用于更新状态、负责人、截止日期、分发时间。
- RndTaskEntity 增加 accept 方法，用于更新状态和接受时间。

服务层调整：

- assignTask 增加事务边界。
- assignTask 在内存任务状态更新后，同步写回 rnd_task：
  - status = PENDING_ACCEPTANCE。
  - assignee_name = 被分发研发人员。
  - due_date = 任务截止日期。
  - assigned_at = 分发时间。
- acceptTask 增加事务边界。
- acceptTask 在内存任务状态更新后，同步写回 rnd_task：
  - status = SAMPLING。
  - accepted_at = 接受时间。

新增测试：

- SampleWorkflowControllerTest 新增 assigningAndAcceptingTaskPersistsTaskStatusToDatabase。
- 测试覆盖：研发总监分发任务后数据库状态为 PENDING_ACCEPTANCE，研发人员接受后数据库状态为 SAMPLING。
- 同时验证负责人、截止日期、分发时间、接受时间均已写回数据库。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：22 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前主链持久化状态：

- 已落库并会随流程更新：样品需求单、样品项目、样品版本、研发任务初始记录、任务分发状态、研发接受状态。
- 仍主要内存：实验单、实验物料明细、内部测试任务/记录、寄样反馈、核价文件、财务通知。

下一步建议：

- 迁移 ExperimentForm 和 ExperimentMaterial，使研发现场填写的实验单草稿能落库。
- 随后迁移 submitExperimentForTest，生成 TestAssignment，并把任务状态更新为 PENDING_TEST。

## 2026-06-18 第九轮执行

已完成“研发现场保存实验单草稿 -> 实验单与物料明细落库”。

新增 JPA 映射：

- ExperimentFormEntity：实验单主表实体。
- ExperimentMaterialEntity：实验物料明细实体。
- ExperimentFormRepository：实验单仓库。
- ExperimentMaterialRepository：实验物料仓库，支持按 experimentFormId 删除旧明细。

服务层调整：

- SampleWorkflowService 接入 ExperimentFormRepository、ExperimentMaterialRepository。
- saveExperimentDraft 增加事务边界。
- 保存实验单草稿后，同步写入 experiment_form。
- 保存实验单草稿后，同步重写 experiment_material：
  - 先按 experiment_form_id 删除旧物料明细。
  - 再按当前草稿物料列表写入最新明细。
- 这样支持研发人员在现场多次保存草稿，避免旧物料明细残留。

新增测试：

- SampleWorkflowControllerTest 新增 savingExperimentDraftPersistsFormAndMaterialDetailsToDatabase。
- 测试覆盖：任务分发并接受后，保存实验单草稿，数据库中能查到 experiment_form 和 experiment_material。
- 同时验证实验单状态、操作人、版本号、物料名称和重量均已落库。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：23 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前主链持久化状态：

- 已落库并会随流程更新：样品需求单、样品项目、样品版本、研发任务初始记录、任务分发状态、研发接受状态、实验单草稿、实验物料明细。
- 仍主要内存：提交内部测试、内部测试任务/记录、锁版、复打样、寄样反馈、核价文件、财务通知。

下一步建议：

- 迁移 submitExperimentForTest：实验单提交测试时，experiment_form 状态写回 SUBMITTED_FOR_TEST，生成 test_assignment，并把 rnd_task 状态写回 PENDING_TEST。
- 随后迁移内部测试通过/不通过，完成实验单锁版和 A1 复打样版本的数据库记录。

## 2026-06-18 第十轮执行

已完成“实验单提交内部测试 -> 实验单状态、研发任务状态、测试任务落库”。

新增 JPA 映射：

- TestAssignmentEntity：内部测试任务实体，记录测试人员、实验单、任务、样品版本、状态和分配时间。
- TestAssignmentRepository：内部测试任务仓库。

实体调整：

- ExperimentFormEntity 增加 submit 方法，用于将实验单状态更新为 SUBMITTED_FOR_TEST，并写入 submitted_at。
- RndTaskEntity 增加 markPendingTest 方法，用于将研发任务状态更新为 PENDING_TEST。

服务层调整：

- SampleWorkflowService 接入 TestAssignmentRepository。
- submitExperimentForTest 增加事务边界。
- 提交实验单给内部测试后，同步写回 experiment_form：
  - status = SUBMITTED_FOR_TEST。
  - submitted_at = 提交时间。
- 同步写回 rnd_task：
  - status = PENDING_TEST。
- 同步新增 test_assignment：
  - experiment_form_id = 当前实验单。
  - task_id = 当前研发任务。
  - version_id = 当前样品版本。
  - tester_name = 配置的内部测试人员。
  - status = PENDING_TEST。

新增测试：

- SampleWorkflowControllerTest 新增 submittingExperimentForTestPersistsFormTaskAndAssignmentToDatabase。
- 测试覆盖：保存实验单草稿后提交内部测试，数据库中 experiment_form 状态变为 SUBMITTED_FOR_TEST，rnd_task 状态变为 PENDING_TEST，并生成 test_assignment。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：24 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前主链持久化状态：

- 已落库并会随流程更新：样品需求单、样品项目、样品版本、研发任务初始记录、任务分发状态、研发接受状态、实验单草稿、实验物料明细、实验单提交测试状态、内部测试任务。
- 仍主要内存：内部测试通过/不通过、测试记录、锁版、复打样、寄样反馈、核价文件、财务通知。

下一步建议：

- 迁移内部测试通过：测试记录落库，测试任务状态写回 PASSED，实验单锁定 LOCKED，研发任务完成 COMPLETED。
- 迁移内部测试不通过复打样：测试记录落库，测试任务状态写回 FAILED_RESAMPLE，自动生成 A1/A2/A3 新版本和复打样研发任务。

## 2026-06-18 第十一轮执行

已完成“内部测试确认 -> 通过锁版 / 不通过复打样”的数据库持久化。

新增 JPA 映射：

- TestRecordEntity：内部测试记录实体，记录测试任务、实验单、测试人员、测试结果、测试意见和测试时间。
- TestRecordRepository：内部测试记录仓库。

实体调整：

- ExperimentFormEntity 增加 lock 方法，用于将实验单状态更新为 LOCKED。
- RndTaskEntity 增加 complete 方法，用于将研发任务状态更新为 COMPLETED。
- TestAssignmentEntity 增加 pass、failForResample 方法，用于写回 PASSED 和 FAILED_RESAMPLE。

服务层调整：

- SampleWorkflowService 接入 TestRecordRepository。
- passInternalTest 增加事务边界。
- 内部测试通过后，同步写回：
  - experiment_form.status = LOCKED。
  - test_assignment.status = PASSED。
  - test_record 新增通过记录。
  - rnd_task.status = COMPLETED。
- failInternalTestForResample 增加事务边界。
- 内部测试不通过复打样后，同步写回：
  - test_assignment.status = FAILED_RESAMPLE。
  - test_record 新增失败复打样记录。
  - 原 rnd_task.status = COMPLETED。
  - sample_version 新增下一版本，如 A0 -> A1。
  - rnd_task 新增下一版本复打样任务，状态为 PENDING_ACCEPTANCE。

新增测试：

- passingInternalTestLocksExperimentVersion 增加数据库断言，验证测试通过后实验单锁定、测试任务通过、测试记录落库、研发任务完成。
- failedInternalTestCreatesNextSampleVersionAndResamplingTask 增加数据库断言，验证测试失败复打样后测试记录落库、原任务完成、新样品版本和新研发任务落库。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：24 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前主链持久化状态：

- 已落库并会随流程更新：样品需求单、样品项目、样品版本、研发任务初始记录、任务分发状态、研发接受状态、实验单草稿、实验物料明细、实验单提交测试状态、内部测试任务、内部测试记录、实验单锁版、复打样新版本、复打样新任务。
- 仍主要内存：寄样反馈、核价文件、财务通知。

下一步建议：

- 迁移寄样反馈模块：shipment_record、customer_feedback 落库。
- 再迁移核价文件和财务通知：pricing_file、finance_notification 落库，为后续 MinIO/NAS 文件归档和飞书通知做准备。

## 2026-06-18 第十二轮执行

已完成“寄样记录 -> 客户反馈”的数据库持久化。

新增 JPA 映射：

- ShipmentRecordEntity：寄样记录实体，记录样品版本、寄样数量、收件人、快递单号、备注、状态和寄样时间。
- ShipmentRecordRepository：寄样记录仓库。
- CustomerFeedbackEntity：客户/业务反馈实体，记录寄样记录、反馈人、反馈结果、反馈意见和反馈时间。
- CustomerFeedbackRepository：客户反馈仓库。

服务层调整：

- SampleWorkflowService 接入 ShipmentRecordRepository、CustomerFeedbackRepository。
- createShipment 增加事务边界。
- 创建寄样记录后，同步写入 shipment_record：
  - status = SHIPPED。
  - version_id = 当前锁版样品版本。
  - tracking_no = 快递单号。
- submitCustomerFeedback 增加事务边界。
- 提交客户反馈后，同步写回 shipment_record 状态：
  - PASSED -> FEEDBACK_PASSED。
  - FAILED_RESAMPLE -> FEEDBACK_FAILED_RESAMPLE。
  - STOPPED -> STOPPED。
- 提交客户反馈后，同步新增 customer_feedback。

新增测试：

- passedSampleCanRecordShipmentGeneratePricingFileAndNotifyFinance 增加数据库断言。
- 测试覆盖：寄样记录落库、寄样反馈后状态写回、客户反馈记录落库。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：24 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前主链持久化状态：

- 已落库并会随流程更新：样品需求单、样品项目、样品版本、研发任务初始记录、任务分发状态、研发接受状态、实验单草稿、实验物料明细、实验单提交测试状态、内部测试任务、内部测试记录、实验单锁版、复打样新版本、复打样新任务、寄样记录、客户反馈。
- 仍主要内存：核价文件、财务通知。

下一步建议：

- 迁移核价文件：pricing_file 落库，支持 A0-核价V1、A0-核价V2 等版本记录。
- 迁移财务通知：finance_notification 落库，为飞书消息卡片和通知状态重试做准备。

## 2026-06-18 第十三轮执行

已完成“核价文件生成 -> 通知财务核价”的数据库持久化。

新增 JPA 映射：

- PricingFileEntity：核价文件版本实体，记录样品版本、核价版本、文件名、状态、文件大小和生成时间。
- PricingFileRepository：核价文件仓库。
- FinanceNotificationEntity：财务通知实体，记录核价文件、财务接收人、备注、通知状态和通知时间。
- FinanceNotificationRepository：财务通知仓库。

服务层调整：

- SampleWorkflowService 接入 PricingFileRepository、FinanceNotificationRepository。
- generatePricingFile 增加事务边界。
- 生成核价文件后，同步写入 pricing_file：
  - pricing_version = A0-核价V1、A0-核价V2 等。
  - file_name = 核价 Excel 文件名。
  - status = GENERATED。
- notifyFinance 增加事务边界。
- 通知财务后，同步写回 pricing_file.status = FINANCE_NOTIFIED。
- 通知财务后，同步新增 finance_notification：
  - recipient_name = 财务接收人。
  - status = SENT。

新增测试：

- passedSampleCanRecordShipmentGeneratePricingFileAndNotifyFinance 增加核价和财务通知数据库断言。
- 测试覆盖：核价文件落库、通知财务后核价文件状态写回、财务通知记录落库。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：24 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前主链持久化状态：

- 已完成数据库闭环：研发内勤创建需求、研发总监审核、生成样品项目和 A0 版本、任务池、任务分发、研发接受、实验单草稿、实验物料、提交内部测试、测试通过锁版、测试不通过复打样生成 A1/A2/A3、寄样、客户反馈、生成核价文件、通知财务。
- 仍需后续建设：正式登录权限、飞书免登与消息卡片、MinIO/NAS 文件存储、实验附件、文件归档中心、正式 Vue 前端、配置中心。

下一步建议：

- 开始做统一文件与归档能力：archive_file、文件上传接口、核价 Excel 文件保存到 MinIO/NAS。
- 或开始做飞书自建应用基础：飞书免登、用户映射、任务通知、财务通知卡片。

## 2026-06-18 第十四轮执行

已开始统一文件与归档能力建设，先完成“核价文件生成 -> 自动归档记录”的数据库闭环。

新增 JPA 映射：

- ArchiveFileEntity：归档文件实体，记录业务类型、业务 ID、样品版本、文件名、归档路径、文件链接、文件状态和归档时间。
- ArchiveFileRepository：归档文件仓库。

服务层调整：

- SampleWorkflowService 接入 ArchiveFileRepository。
- generatePricingFile 仍保持事务边界。
- 核价文件生成后，在写入 pricing_file 的同时新增 archive_file：
  - business_type = PRICING_FILE。
  - business_id = pricing_file.id。
  - version_id = 当前样品版本。
  - file_name = 核价 Excel 文件名。
  - file_path = 样品编号/版本号/核价/文件名。
  - file_status = ARCHIVED。

新增测试：

- passedSampleCanRecordShipmentGeneratePricingFileAndNotifyFinance 增加归档断言。
- 测试覆盖：核价文件生成后，archive_file 自动出现对应归档记录，并按样品编号、版本号、核价目录形成稳定路径。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：24 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前归档能力状态：

- 已完成：核价文件元数据自动归档。
- 仍需后续建设：真实 Excel 文件落盘、MinIO/NAS 上传、实验照片/测试附件归档、归档文件列表接口、按样品项目查看所有归档文件。

下一步建议：

- 实现本地/MinIO 文件存储抽象，把核价 Excel 的字节内容保存成真实文件，并将 file_path/file_url 写回 archive_file。
- 随后增加归档查询接口：按 sample_version 或 sample_project 查看所有需求、实验、测试、寄样、核价文件。

## 2026-06-19 第十五轮执行

已完成“核价 Excel 字节内容 -> 本地归档文件”的真实落盘能力。

新增服务：

- LocalArchiveStorageService：本地归档文件存储服务。
- 默认根目录：target/rnd-archive。
- 支持按相对路径创建目录并写入文件内容。
- 增加路径穿越保护，避免归档路径逃出根目录。

服务层调整：

- SampleWorkflowService 接入 LocalArchiveStorageService。
- generatePricingFile 生成 Excel 后，将 PricingFileService 返回的字节内容传入归档流程。
- persistPricingArchive 写入 archive_file 之前，先把 Excel 文件保存到：
  - target/rnd-archive/样品编号/版本号/核价/文件名。
- archive_file.file_path 继续保存业务相对路径，便于后续切换 MinIO/NAS 时保持业务路径稳定。

新增测试：

- passedSampleCanRecordShipmentGeneratePricingFileAndNotifyFinance 增加真实文件断言。
- 测试覆盖：
  - archive_file 归档记录存在。
  - target/rnd-archive 下真实 Excel 文件存在。
  - 文件大小等于 pricing_file.content_length。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：24 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前归档能力状态：

- 已完成：核价文件元数据归档、核价 Excel 本地真实文件落盘。
- 仍需后续建设：MinIO/NAS 适配、文件下载接口、归档列表接口、实验照片/测试附件上传归档。

下一步建议：

- 增加归档查询接口：按样品版本或业务 ID 查询归档文件。
- 增加核价 Excel 下载接口，让财务可以通过系统或飞书卡片下载核价文件。

## 2026-06-19 第十六轮执行

已完成“归档文件查询 + 核价 Excel 下载”的后端接口闭环，财务后续可以从系统或飞书卡片入口下载已生成的核价文件。

新增模型：

- ArchiveFileView：归档文件接口视图，返回归档 ID、业务类型、业务 ID、样品版本、文件名、路径、文件状态和归档时间。

新增接口：

- GET /api/v1/sample-versions/{id}/archive-files
  - 按样品版本查询归档文件列表。
  - 当前可返回已归档的核价 Excel 文件。
- GET /api/v1/archive-files/{id}/download
  - 按归档文件 ID 下载真实文件内容。
  - 当前用于下载本地归档的核价 Excel。

服务层调整：

- ArchiveFileRepository 增加 findByVersionIdOrderByArchivedAtDesc。
- ArchiveFileEntity 增加 toView 映射和文件名、文件路径读取方法。
- LocalArchiveStorageService 增加 read 方法：
  - 从 target/rnd-archive 读取真实归档文件。
  - 保留路径穿越保护。
  - 文件不存在时返回业务异常 ARCHIVE_FILE_NOT_FOUND。
- SampleWorkflowService 增加：
  - archiveFiles(versionId)：查询版本归档文件。
  - downloadArchiveFile(archiveFileId)：读取归档文件内容。

新增测试：

- passedSampleCanRecordShipmentGeneratePricingFileAndNotifyFinance 增加归档接口断言。
- 测试覆盖：
  - 样品版本归档列表可查到核价 Excel。
  - 归档视图包含业务类型、业务 ID、文件名、归档状态。
  - 下载接口返回的字节内容等于本地真实 Excel 文件内容。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：24 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前归档能力状态：

- 已完成：核价文件元数据归档、核价 Excel 本地真实文件落盘、按样品版本查询归档文件、按归档 ID 下载文件。
- 仍需后续建设：MinIO/NAS 适配、实验照片上传归档、测试附件上传归档、正式前端归档中心、飞书卡片下载链接。

下一步建议：

- 开始做“实验附件/现场照片上传归档”，把研发现场照片、称重照片、成品照片纳入 archive_file。
- 或进入飞书自建应用基础能力：飞书免登、用户映射、任务通知、财务通知卡片。

## 2026-06-19 第十七轮执行

已完成“实验附件/现场照片上传归档”的后端闭环，研发现场照片、称重照片、成品照片等文件可以挂到实验单，并进入样品版本归档列表。

新增接口：

- POST /api/v1/experiment-forms/{id}/attachments
  - multipart/form-data 上传。
  - file：上传文件内容。
  - fileName：可选，前端可指定展示/归档文件名。
  - 返回 ArchiveFileView。

服务层调整：

- SampleWorkflowService 增加 archiveExperimentAttachment：
  - 根据实验单 ID 定位样品编号、样品版本和版本号。
  - 将附件保存到 target/rnd-archive/样品编号/版本号/实验附件/归档ID/文件名。
  - 同名附件会进入不同归档 ID 目录，避免覆盖历史文件。
  - 新增 archive_file 记录：
    - business_type = EXPERIMENT_ATTACHMENT。
    - business_id = experiment_form.id。
    - version_id = 实验单对应样品版本。
    - file_status = ARCHIVED。
- ExperimentFormEntity 增加只读 getter，支持服务重启后从数据库定位实验单归档上下文。
- 附件文件名做基础清理，避免前端传入路径片段影响归档目录。

新增测试：

- uploadingExperimentAttachmentArchivesFileAndAllowsVersionDownload。
- 测试覆盖：
  - 实验单草稿阶段可上传附件。
  - 上传后生成 EXPERIMENT_ATTACHMENT 归档记录。
  - 归档路径按“样品编号/A0/实验附件/归档ID/文件名”生成。
  - 按样品版本查询归档列表能看到附件。
  - 按归档 ID 下载文件，内容与上传内容一致。
  - 同一实验单上传同名附件时不会覆盖前一次归档文件。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：25 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前归档能力状态：

- 已完成：核价文件归档/下载、实验附件上传归档/下载、按样品版本查看归档列表。
- 仍需后续建设：MinIO/NAS 适配、文件类型/大小限制、附件分类字段、正式前端归档中心、飞书卡片下载链接。

下一步建议：

- 增加上传限制和附件元数据：附件分类（称重照片/过程照片/成品照片/测试附件）、上传人、备注、文件大小、contentType。
- 或开始飞书基础集成：飞书免登、用户映射、任务分发通知卡片。

## 2026-06-19 第十八轮执行

已完成附件元数据和基础上传限制，研发现场上传的照片/附件现在不只是“有文件”，还能被前端按类型、上传人、备注和文件属性展示。

数据库调整：

- archive_file 增加可选字段：
  - category：附件分类，如 WEIGHING_PHOTO、PROCESS_PHOTO、FINISHED_PHOTO、TEST_ATTACHMENT。
  - uploaded_by：上传人。
  - remark：附件备注。
  - content_type：文件 MIME 类型。
  - file_size：文件大小。

接口调整：

- POST /api/v1/experiment-forms/{id}/attachments 增加可选参数：
  - category。
  - uploadedBy。
  - remark。
- ArchiveFileView 增加返回字段：
  - category。
  - uploadedBy。
  - remark。
  - contentType。
  - fileSize。

服务层调整：

- SampleWorkflowService.archiveExperimentAttachment 保存附件元数据。
- 附件大小限制为 10MB，超过时返回业务异常：
  - code = ARCHIVE_FILE_TOO_LARGE。
- 空文件仍返回：
  - code = ARCHIVE_FILE_EMPTY。
- 核价文件归档继续兼容，新增元数据字段可为空。

新增测试：

- uploadingExperimentAttachmentArchivesFileAndAllowsVersionDownload 扩展断言：
  - 上传时传入 category、uploadedBy、remark。
  - 接口返回和数据库均保存 category、uploadedBy、remark、contentType、fileSize。
  - 归档列表可返回附件分类和上传人。
- rejectsExperimentAttachmentWhenFileIsTooLarge：
  - 超过 10MB 的附件上传被拒绝，返回 ARCHIVE_FILE_TOO_LARGE。
- SchemaMigrationTest 增加 archive_file 新列存在断言。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：26 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前归档能力状态：

- 已完成：核价文件归档/下载、实验附件上传归档/下载、附件元数据、10MB 大小限制、按样品版本查看归档列表。
- 仍需后续建设：MinIO/NAS 适配、文件类型白名单、前端归档中心、飞书卡片下载链接。

下一步建议：

- 增加文件类型白名单配置：图片、Excel、PDF 等按业务场景控制。
- 或进入飞书基础集成：飞书免登、用户映射、任务分发通知卡片。

## 2026-06-19 第十九轮执行

已完成附件文件类型白名单校验，研发现场上传文件现在会按 MIME 类型做基础安全过滤。

白名单规则：

- 允许图片：
  - image/jpeg。
  - image/png。
  - image/webp。
- 允许 PDF：
  - application/pdf。
- 允许 Excel：
  - application/vnd.ms-excel。
  - application/vnd.openxmlformats-officedocument.spreadsheetml.sheet。

服务层调整：

- SampleWorkflowService.archiveExperimentAttachment 增加 contentType 白名单校验。
- contentType 为空或不在白名单内时返回业务异常：
  - code = ARCHIVE_FILE_TYPE_NOT_ALLOWED。
- 当前白名单放在服务层常量中，后续配置中心上线后可迁移到系统参数或数据库配置。

新增测试：

- validatesExperimentAttachmentContentTypeWhitelist：
  - PDF 可以上传。
  - Excel xlsx 可以上传。
  - application/x-msdownload 类型文件会被拒绝。

验证结果：

- JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test：27 tests, 0 failures, 0 errors。
- node prototype-app/test-prototype.mjs：30 views, 29 navigation targets, 5 top-level modules。

当前归档能力状态：

- 已完成：核价文件归档/下载、实验附件上传归档/下载、附件元数据、10MB 大小限制、文件类型白名单、按样品版本查看归档列表。
- 仍需后续建设：MinIO/NAS 适配、正式前端归档中心、飞书卡片下载链接、白名单配置中心化。

下一步建议：

- 开始飞书基础集成：飞书免登、用户映射、任务分发通知卡片。
- 或先做前端归档中心，把后端归档能力在原型/正式前端中展示出来。
