# 研发工作台与导出验收

最新验收日期：2026-09-24（Asia/Shanghai）。文档文件名沿用计划日期 2026-09-15。

## 最新状态 — 限定复审完成，7 项关闭、1 项预览提示待补齐

本轮针对 7 项 Important 与 1 项 Minor 完成修复与验证；独立限定复审结论为 **7 项 ADDRESSED、1 项 PARTIAL**，不能宣称全部关闭。后端 **364/364**、前端 **159/159**，契约、路由、类型检查及 PC/移动端构建通过；已有真实会话 API **152/152** 证据保留。最新 CUA 确认缺失实测保持“待补充”、95→97℃ 显示“+2℃”、1091px 页面及步骤区不再横向溢出、控制操作完整可见；A/B 紧凑概览与继承来源导出排版通过。代码提交为 `39770f5`。

预览已更新且 8082/5183/5184 健康，飞书关闭。剩余 P2：SOP 正式生成已拦截参数名称/单位等缺项，检查弹窗也显示问题，但下载的不完整预览文档尚未带入同一检查结果；无名称的参数可能整项隐藏，缺单位的参数没有对应待补充提示。建议后续把 SOP 检查 issues 传入预览渲染，并补两种缺项的 DOCX 内容断言，继续允许不完整预览。此问题未在本次限定复审后继续修改。另：浏览器 download 事件接收尚无成功证据，不能以 API 文件验证替代。

以下“Task 6 历史基线”及其测试/未关闭事项描述当时状态，**其中缺失汇总与布局问题已由末节最终修复关闭**，不代表最新代码仍未修复。

## Task 6 历史基线（2026-09-22）

本轮后端 360/360、前端 155/155、初次真实本地 API 验收 149/149 通过；复审补充断言后的最新 API 验收为 **152/152**（见末节）。契约、路由、类型检查、PC/移动端生产构建通过。**不能据此宣称全部 UI 验收完成**：主任务 CUA 实测发现缺失重量汇总仍显示 0.000kg 的问题，尚未修复；独立最终审查仍是后续关卡。本任务没有修改生产代码。

预览地址：[PC 研发工作台](http://127.0.0.1:5183/admin/)、[移动端](http://127.0.0.1:5184/m/)。只操作专用 8082/5183/5184 预览服务，未触碰 8080/5173。以当前生产代码基线 `12161a9` 重建后验收；最终三个健康检查均为 HTTP 200。Feishu 在 preview 配置中禁用，脚本不调用财务通知或外部消息接口。

## 可复跑 API 验收

脚本：`scripts/verify-rnd-trials.mjs`。必须从项目根目录运行，仅接受固定 `http://127.0.0.1:5183/api/v1`，使用 `RND_PREVIEW_PASSWORD` 传入本地已配置 bootstrap 密码。此次通过读取既有 bootstrap 配置、仅在子进程环境中注入凭据运行；结果文件不保存密码、会话或预览 token。

```sh
node --check scripts/verify-rnd-trials.mjs
# RND_PREVIEW_PASSWORD 由当前本地测试凭据配置注入，不写入命令历史或报告
node scripts/verify-rnd-trials.mjs
```

最终日志 `/private/tmp/task6-api.log`：`RESULT 149/149`，exit 0。

最终证据目录：`data/preview/Task6-2026-09-22T11-09-56-390Z/`，其中 `results.json` 记录每条断言、测试对象和制品标识；其余为通过真实下载接口取得的 XLSX/DOCX。149 是具名边界/行为断言组数量，不是所有底层 `assert` 调用数量。

- 新建软件测试记录：`TASK-0016`、`EXP-0016`、`VER-0016`，不会修改已有业务记录。
- 正式来源：`PREV-4edf6dc4-5529-416a-9477-022692ed39e6`，正式 R2，提交时 A 方案 V4。
- A：`TRIAL-449ac88e-8e73-4d8a-82d4-920428c250df`；B：`TRIAL-e667f196-db21-49cf-be68-c513d7699249`。
- 核价：`PRICE-0008`，留在 `PENDING_PRICING_REVIEW`，未审核/通知财务。
- 中间调试运行创建的 `TASK-0010` 至 `TASK-0015` 也是清楚标记的独立软件测试记录，保留用于追溯，没有删除任何业务记录。`TASK-0010` 已交给主任务进行浏览器交互验收，后续操作不影响最终 149/149 证据。

所有温度、时间、重量及食品工艺参数只是软件测试例子，**不代表经验证的生产标准或食品工艺批准**。

## 验收范围与证据

| 范围 | 实际验证 |
| --- | --- |
| A/B 独立性 | 独立保存、重新 GET，A 实测 100kg、计划 110kg；B 计划 99.8kg、实测 null；复制不以计划替代实测 |
| 版本与归档 | 过期保存和确认冲突；归档禁止提交预览、恢复后可用；导出旧版本返回 `PROCESS_EXPORT_VERSION_CONFLICT` |
| 可信控制 | 客户端伪造确认被清除；未确认 critical 控制阻断晋升；确认身份来自会话；继承实测不可重新确认 |
| 显式晋升 | 实际工艺检查通过后仍需 `confirmed:true`；正式草稿版本变化使旧预览失效；同幂等键重试只返回原版本 |
| 覆盖保护 | 覆盖前的完整、有物料流转的工艺草稿可原样读取恢复；正式来源只含受控来源元数据，不返回整个试验草稿 |
| 只读预览 | 三类空草稿/试验预览可下载；任务详情、审计和成果列表前后不变，没有成果归档或批准副作用 |
| 新正式文件门槛 | 缺操作说明的已提交版本可有配方，但新 SOP `export-check` 不通过，直接生成也被拒且成果列表为空；包装缺数量单位阻断核价生成 |
| 固定来源 | 后续修改 A 名称/实际重量不改变既有 revision/source；已生成 SOP/配方下载 SHA-256 不变；三个正式文件均标识同一 R2 / A V4 |
| 数据口径 | 外部投入总计 102kg（内部中间料不重复）；主料 100→90→72kg，对应 72%；独立成品 74kg / 74袋，不从得率推算；配方两表合计分别精确为 102 与 100 |
| 缺失与零 | 空实际预览明确待填写；NONE 非分层观察无数据保持待填写，实录零保持数值零；不为了零观测绕过分层流转正投入规则 |
| 金额隔离 | 正式核价、SOP、配方不出现单价、金额、成本、毛利、税率、税费、利润、自动报价字段（税费/自动报价由复审补齐） |
| 中间流转边界 | 正式 snapshot 的 STEP_OUTPUT 仅指向本版本产出，materialCode/formulaMaterialId 为 null；完整流程前后实验单外部投料列表不变，中间产物不混入；当前无独立物料主数据实体/读取接口，不冒称完成物料库主数据接口对比 |
| 生命周期 | 送测后保存、复制、归档、提交预览均拒绝；测试通过锁定后保存继续拒绝 |
| 包装 | 显式填写数量单位、规格、换算规则；正式核价保持独立成品来源，试验核价预览不能借用实验单成品产量 |

真实会话使用研发内勤、研发总监、研发工程师、测试员、财务和管理员：内勤新建需求/核价，总监审批分派，工程师保存/确认/晋升/生成，测试员仅可读取当前任务固定正式版本及其下载文件，财务不能获取试验草稿或未获授权的核价文件。测试员读取另一个旧正式版本/source/成果下载均拒绝；测试员和财务不能读 full trial JSON 或 displaced draft。未登录被拒。管理员只读审计验证预览无副作用。无第二个测试员账号的真实会话轮换；精确授权的服务集成测试同时覆盖未分配、归档分配及额外角色场景。

## 回滚回归

`TrialPromotionServiceTest` 16/16，通过真实 JDBC/Flyway/事务：

1. 原储存校验失败用例的被覆盖草稿不再为空，而包含两步骤、内部产出引用、`ProcessInput.sourceStepMaterialId` 引用及控制观测。
2. 新用例通过仅测试数据库约束，在正式 revision 已创建后的最终 promotion INSERT 精确注入失败，验证图、状态/版本、revision、promotion、审计及试验记录均恢复；去掉故障后同一预览/请求可成功，displaced draft 等于原图。

没有发现需要修改事务生产实现的缺陷。最初两次失败为新夹具仍携带外部物料编码和重用固定图 ID 的构造错误；修正夹具并使用现有真实 remapper 分配 ID 后通过。它们不是生产缺陷 RED，也没有通过放松校验解决。

## 最终命令与结果

| 工作目录 / 命令 | 2026-09-22 结果 | 日志 |
| --- | --- | --- |
| backend：`mvn -q -Dtest=TrialPromotionServiceTest test` | 16 tests，0 failures/errors/skipped | `/private/tmp/task6-rollback-tests.log` |
| backend：`mvn -q test` | 360 tests，0 failures/errors/skipped | `/private/tmp/task6-backend-full.log`；Surefire XML 汇总 |
| frontend：`node --experimental-strip-types --test tests/*.test.mts` | 155 tests，155 pass，0 fail/cancelled/skipped/todo | `/private/tmp/task6-frontend-full.log` |
| frontend：`pnpm check:api-contracts` | 通过 | `/private/tmp/task6-contracts.log` |
| frontend：`pnpm check:routes` | PC 30 / mobile 16，通过 | `/private/tmp/task6-routes.log` |
| frontend：`pnpm typecheck` | shared / PC / mobile 通过 | `/private/tmp/task6-typecheck.log` |
| frontend：`pnpm build` | PC / mobile production builds 通过 | `/private/tmp/task6-frontend-build.log` |
| 根目录：`node scripts/run-rnd-preview.mjs stop` → `build` → `start` → `status` | 最新运行时；8082/5183/5184 200 | `/private/tmp/task6-preview-build.log` |
| 根目录：`node scripts/verify-rnd-trials.mjs`（本地配置凭据仅环境注入） | 149/149，exit 0 | `/private/tmp/task6-api.log` |
| 根目录：`git diff --check` | 通过 | 无空白错误 |

通过不等于日志无警告：沿用 Flyway/H2 版本兼容提醒、JVM native/Mockito 警告、Vue 测试夹具 Router 提醒及 Vite chunk-size 提示；故障注入测试会有预期异常日志。未升级依赖或压制警告。

## 实际文件视觉 QA（沿用主任务已完成证据）

本轮未改 renderer，不重复渲染未变化的输出代码。主任务此前用真实应用生成文件及捆绑文档/表格运行时完成检查：

- 核价：`backend/target/task-4-export-qa/formal-basis-102kg-72percent-74bags.xlsx`、`trial-incomplete-preview.xlsx`、`long-chinese-37-ingredient-basis.xlsx`。最终 3+3+6=12 个打印页全部检查；3 个正式表可视导入检查通过，无重叠/裁切，金额列不存在。渲染目录 `/private/tmp/rnd-export-qa-0922.8zDblM/formal-v2`、`print-v2`。
- SOP：`backend/target/task-5-export-qa/two-major-control-heavy-rnd-sop-v4.docx`，全部 **4 页**逐页检查，目标/范围并列、缺失/零、相邻控制、重复表头、页脚均可读。目录 `/private/tmp/rnd-export-qa-0922.8zDblM/sop-v4`。
- 配方：`backend/target/task-5-export-qa/long-30-material-rnd-formula-v4.xlsx`，**本次实际投料 / 每100kg折算两个工作表**，全部 **6 打印页**逐页检查，正确重复各表表头、102/100合计、中文长名与数字无裁切。目录 `/private/tmp/rnd-export-qa-0922.8zDblM/formula-v4`。另有 `two-major-102kg-rnd-formula-v2.xlsx` 代表性短表证据。

## 浏览器与未关闭事项

开始时旧 IAB `data:errorpage` 绑定受 URL policy 阻止，未绕过。其后主任务观察到旧 tab 消失、浏览器可用这一外部状态变化，正常打开授权本地 URL，通过 CUA 登录研发账户继续真实验收。因此旧浏览器阻塞不是当前唯一结论，但也不能用 API 下载替代浏览器点击下载证据。

主任务 CUA 实测通过项（`TASK-0010`）：

- 登录与刷新正常；B 目的修改为“界面验收：B方案独立修改并刷新”，切换 A 出现取消/放弃/保存并切换三选项；保存并切换后 B V5、A 目的仍为“A 独立保存”；整页刷新后 B 修改持久化。
- A/B 基线比较把缺实测显示为 `90→—`、`80→—`，不是零；步骤输入的实际 DOM value 为空，虽然辅助树文字可能误读为零，不能将其认作输入框错误。
- 预览弹窗显示三类真实缺项；未保存状态禁用预览按钮。点击配方下载没有页面错误，但 CUA 等待 download 事件 3 秒超时，**浏览器实际收到文件尚未确认**；真实 API 文件下载已另行验证通过。
- A 正式提交弹窗展示服务端校验、物料平衡差警告、变更原因和明确确认 checkbox；未勾选时确认按钮禁用。检查后取消，没有额外正式提交。

未关闭 UI 发现：

1. 结构复制 B 的实际投入/产出均缺失，大工序得率正确显示待补充，但下面卡片仍显示“主料投入 0.000kg / 末端产出 0.000kg / 平衡差 0.000kg”；小步骤“配方汇总 外部物料 0.000kg”同样把缺失表示为零。需最终定向修复，不影响本轮已验证的导出 actual-null 保留事实。
2. 当前约 1091px 宽 IAB，三个长方案名（测试 timestamp）撑宽试验区，使保存/预览/提交按钮和工序得率被挤到右侧、整页横向溢出。长方案名/窄窗口布局需要收尾。
3. 浏览器 download 事件尚未确认，不将点击无错误等同于浏览器下载通过。

按照主任务指示，本轮不扩展生产修复；以上交由最终独立审查与后续定向修复。

## 复审补充验收 — 2026-09-22

基于 `1eea0f8`，只补验收脚本和报告，没有生产代码改动。正式配方/SOP/核价检查均显式增加“税费”“自动报价”禁词。代码核对确认没有独立物料主数据实体/接口，因此审查方撤回要求 master API 前后对比的发现；没有新建范围外物料库。保留准确的现有边界检查：新记录的唯一中间产物名只存在于工艺产出/引用链，正式引用无外部物料编码/配方物料 ID，`GET /rnd-tasks/{id}/detail` 的 `currentExperimentForm.materials` 在整个流程前后深度相等且没有该中间产物名。

- 一次覆盖重跑：本地配置凭据环境注入后 `node scripts/verify-rnd-trials.mjs` → `152/152`，exit 0，日志 `/private/tmp/task6-fix1-api.log`。
- 最新软件测试记录：`TASK-0017` / `EXP-0017`，核价 `PRICE-0009`；证据 `data/preview/Task6-2026-09-22T11-22-00-243Z/results.json` 及其同目录真实下载文件。
- `node --check scripts/verify-rnd-trials.mjs`、`git diff --check` 均通过；依照复审范围不重跑未变化的后端/前端全套，不重复渲染文件。
- 浏览器第二次等待下载事件 15 秒仍超时、无 console 错误；不能宣称浏览器文件接收通过。该限制及 UI 显示/长方案名布局问题仍留最终审查波次处理。

## 最终整体验收修复波次 — 2026-09-22

基于 `508dc59` 的单次定向修复覆盖最终审查的 7 项 Important 与 1 项 Minor：SOP 正式新生成的控制要求/参数含义及适用单位检查、删除图节点时清理对应计划键、导出继承实测来源、紧凑 A/B 并排概览、95→97℃ 的 +2℃ 偏差、缺失汇总待补充（保留实录 0 和 0.0001kg）、长方案名容器内滚动/操作换行、复测 PASS/FAIL 中文标签。正式来源读取晋升时不可变快照，未放松后端引用、控制、权限或提交校验；旧归档下载不增加新完整性门槛。

最新整套结果取代此前 360/155 的计数：后端 **364/364**、前端 **159/159**，API contracts、PC 30/mobile 16 routes、shared/PC/mobile typecheck、PC/mobile production build 均通过。日志 `/private/tmp/final-wave-backend-full.log`、`final-wave-frontend-full.log`、`final-wave-frontend-build.log`。一次真实会话 API 回归 **152/152**，日志 `/private/tmp/final-wave-api.log`；测试记录 TASK-0018 / EXP-0018，证据 `data/preview/Task6-2026-09-22T11-49-19-952Z`。预览按项目 stop/build/start/status 重建，8082/5183/5184 均 200，飞书关闭。

新增真实应用导出在 `backend/target/final-fix-export-qa/`：`inherited-qualitative-numeric-sop.docx`、`inherited-formula_xlsx.xlsx`、`inherited-pricing_xlsx.xlsx`。主任务实际渲染并逐页检查 **SOP 4 + 配方 2 + 核价 3 = 9 页**；继承来源不冒充本次新观测，定性要求不强塞数值，数值目标/范围及复测中文可读，无新裁切或重叠。配方两个 sheet、核价三个 sheet 可视导入无错误 token；已知空 shared-string 导入器显示问题不影响实际文件打印空白。目录 `/private/tmp/rnd-export-qa-0922.8zDblM/final-{sop,formula,pricing}`。

2026-09-24 最新实屏复测通过：IAB viewportWidth=documentWidth=1091；步骤区 clientWidth=scrollWidth=519，控制卡及“添加实测”均在可视父容器内，产出/控制操作无裁切。B 第一处计划95/实际97显示“实际97℃ / 偏差+2℃”，缺失主料/外部物料仍显示待补充；A/B 概览 A 102kg/72%、B 缺失状态并排清楚，详细差异默认折叠。删除计划节点保存由真实组件 mounted 回归覆盖，未对持久记录执行现场删除。继承配方页签与右页脚统一为“方案实际投料”，更新的两个打印页及两个 sheet 再次检查通过，最新配方 QA 目录为 `final-formula-v2`。

UI 稳定后于 2026-09-24 再执行一次最终全套：后端 364 tests、前端 159 tests，均 0 failures/errors/skipped；contracts/routes/typecheck/PC+mobile build 全部 exit 0。最终日志为 `/private/tmp/final-wave-backend-final-0924.log`、`final-wave-frontend-final-0924.log`、`final-wave-contracts-final-0924.log`、`final-wave-routes-final-0924.log`、`final-wave-typecheck-final-0924.log`、`final-wave-build-final-0924.log`。这组结果覆盖最后的步骤网格修复；前面的 2026-09-22 全套和 API 是可追踪基线。

浏览器 download 事件超时仍诚实保留，API 字节下载/文件内容通过不替代浏览器实际收件证据；没有为此盲改下载处理器。逐项 RED/GREEN 见 `.superpowers/sdd/2026-09-15-rnd-workbench-and-exports/final-fix-report.md`。主任务将在本轮提交后执行唯一一次最终限定范围复审，不把尚未执行的复审计为通过。
