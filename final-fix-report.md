# 最终工艺工作流修复报告

日期：2026-08-21

基线：`d724c17`（包含 Task 8 Chrome 7/7 验证）

提交：`fix: close final process workflow gaps`

## 结论

`final-review.md` 的 7 个 Important 已逐项关闭。正式工艺、配方、SOP、核价和送测现在共用同一套工艺版本、主料链与物料边界规则；所有对身份敏感的写动作均以服务端会话为准。

## 逐项关闭说明

1. **大工序物料平衡遗漏早期旁路产出**
   - 根因：后端和前端只汇总最后一个有产出的步骤。
   - 修复：按整个大工序收集全部步骤产出，排除已经被下游 `STEP_OUTPUT` 消费的中间产物，剩余终端产出（成品、留样、余料、尾料、废弃等）全部进入物料平衡。
   - 一致性：正式校验、前端预览和 SOP 均使用相同计算语义；SOP 仍逐步展示全部产出。
   - 覆盖：10kg → 9kg 主线 + 1kg 前段废弃，继而 9kg → 8kg 成品 + 1kg 后段废弃，平衡差为 0。

2. **主料链允许分叉或跳链**
   - 根因：旧校验只验证引用存在、顺序和类型，没有验证唯一消费者和“当前链尖”。
   - 修复：每个主料流转步骤最多一个主料投入/产出；主料产出最多一个主料消费者；后续主料投入必须承接最近一次有效主料产出。无主料变化的普通操作步骤仍可穿插，非主料旁路仍可使用辅助产出。
   - 覆盖：分叉、回用旧链尖、多主料投入/产出、缺一端均被正式校验和前端预览拒绝。

3. **空 `NONE` 工序和 0kg 主料可正式提交**
   - 根因：`NONE` 提前返回，重量完整性只判断非空。
   - 修复：`NONE` 仍必须有实际步骤和业务原因；包装/静置等无得率工序通过明确原因合法豁免。需计算得率的大工序主料首投和末产都必须大于 0，避免 `null yield` 静默进入成品得率。无分层步骤的旧结构也执行同一套 `> 0` 规则，不再跳过 0kg 大工序。
   - 覆盖：空 `NONE`、无原因、分层及旧结构的 0kg 主料投入/产出被拒绝；有步骤且有原因的无得率工序可提交。

4. **极重要偏差确认身份可由客户端伪造**
   - 根因：草稿保存原样持久化 `resolved/confirmedBy/confirmedAt`。
   - 修复：普通保存会清除客户端伪造字段；无偏差的极重要控制点可由当前有权研发人员请求确认，但确认人和时间由服务端会话及服务器时间覆盖。偏差闭环增加研发总监专用确认接口，要求偏差处理完整且复测 `PASS`，并写独立审计日志。工程师不能确认偏差。
   - 前端：确认人不再可编辑；偏差确认调用可信接口；控制点和测量记录自动补稳定 ID。

5. **旧实验单写接口存在跨用户 IDOR/同名冒用**
   - 根因：草稿、送测、通过/打回依赖请求中的姓名和角色级路由权限。
   - 修复：草稿保存和送测要求服务端会话，研发工程师按 `assignee_user_id` 精确匹配，研发总监可管理；UI 只发送 `AUTO_ASSIGN`，服务端仅从 ACTIVE `TESTER/QA_TESTER` 账号中唯一解析，研发姓名不会被当成测试员或隐式回退。测试任务绑定唯一 `tester_user_id`，通过/打回按会话用户 ID 验证并使用可信姓名记审计。请求中的 `operatorName/testerName` 不再决定执行身份。
   - 覆盖：同名但不同用户 ID 的研发和测试人员均无法越权。
   - 角色合同：研发总监始终可以保存、提交和送测；研发工程师仅可操作本人任务；研发内勤保持只读，前后端均不再显示或返回不可执行的送测动作。

6. **送测可使用非正式或过期工艺**
   - 根因：后端只校验旧实验单；PC/mobile 对已有表单不会强制保存最新编辑。
   - 修复：送测事务锁定工艺计划和实验单，要求当前状态为 `SUBMITTED` 且存在最新正式 revision；测试任务持久绑定 `process_revision_id`。测试详情、正式版本列表和成果文件下载均以该绑定为权威源，送测后禁止再创建/提交漂移版本。内存缓存丢失时从数据库重建任务，旧任务未绑定版本则返回明确提示。PC 和 mobile 在每次送测前取消延迟自动保存并同步保存当前草稿；PC 同时 flush 分层工艺工作台，保存失败即停止送测。
   - 并发与迁移：提交和测试判定使用数据库悲观锁；V24 增加正式 revision FK、测试用户 FK，以及每张实验单唯一测试任务约束。并发通知只有一个成功，并发通过/打回只有一个终态和一条测试记录。V23 旧数据升级后 assignment 与 test record 均保留，新绑定字段保持 nullable；旧任务在详情、通过和打回入口都返回明确兼容错误，且不改变任何业务状态。
   - 重启恢复：测试任务、样品版本和研发任务均可从数据库恢复；直接通过或打回不再依赖内存预热。新测试记录、版本和任务使用 32 字符以内的随机唯一 ID，避免服务重启和多实例并发时的序列碰撞；缓存只在事务提交成功后更新。

7. **同一物料跨步骤角色混用导致后续核价失败**
   - 根因：配方按物料聚合，但核价额外要求所有来源角色完全相同。
   - 修复：配方、正式 Formula 和核价统一使用 canonical 角色优先级 `PRIMARY > PROCESS_WATER > AUXILIARY`；相同物料作为主料和辅料投入时合并为一行并以主料作为得率/核价角色。真实的物料映射歧义、编码映射冲突、单位或成本属性冲突仍拒绝。
   - 覆盖：混合角色正式配方和核价成功，已有编码/单位冲突测试继续通过。

## TDD 证据

- 新增用例先在原实现上复现失败：早期终端产出差 1kg、主料分叉仍可提交、0kg/空 `NONE` 可提交、客户端伪造确认被接受、同名测试人可通过、无正式 revision 可送测、混合角色核价报冲突。
- PC/mobile “送测前强制保存”合同与 mounted 场景先失败，再以最小修改转绿。
- 最终审查额外发现控制点缺少既有 ID 时确认接口无法寻址；新增用例先 RED（`point.id` 为空），修复标准化逻辑后 GREEN。
- 独立复审追加的五个场景也逐一 RED→GREEN：研发姓名误作测试员、测试视图漂向 latest revision、旧结构 0kg 被跳过、服务重启后任务丢失、并发请求产生重复送测或双终态。
- 独立复审收口阶段继续覆盖角色合同不一致、旧任务通过/打回绕过正式版本、无内存预热直接通过/打回、历史编号碰撞以及数据库字段长度边界；均先复现失败后以最小修复转绿。

## Fresh 验证

- 后端：`mvn -q test` — **286 tests，0 failures，0 errors，0 skipped**。
- 前端：`node --test tests/*.test.mts` — **91 tests，91 pass，0 fail**。
- 类型：shared `tsc`、PC `vue-tsc`、mobile `vue-tsc` — 全部退出码 0。
- 合同：API contract PASS；路由检查 PASS（PC 30、mobile 16）。
- 构建：PC Vite PASS（3319 modules）；mobile Vite PASS（458 modules）。
- 迁移：SchemaMigrationTest PASS，fresh V1→V24 与 V23→V24 均验证；显式验证旧 assignment/test record 保留、两个新增绑定列保持 nullable、两个 FK、索引和单实验单唯一 assignment 约束。
- 差异：`git diff --check` PASS。

## 已知环境风险

- 当前环境没有可连接的 PostgreSQL（`pg_isready` 返回 `/tmp:5432 - no response`）；V24 已在 H2 PostgreSQL mode 的完整 Flyway 链验证，尚未在 live PostgreSQL 重跑。
- PC 构建仍有既有的单 chunk 大于 500kB 警告，不影响构建成功，与本修复无关。
- 基线 `d724c17` 已有真实 Chrome 7/7；本修复波次对新增交互使用前端合同、mounted 测试、类型检查和双端生产构建验证，未另起一轮真实浏览器全流程。

## 提交排除

未纳入 `final-review.md`、各 task review、`node_modules`、生成的 `.js/.tsbuildinfo`、`test-results` 或构建产物。
